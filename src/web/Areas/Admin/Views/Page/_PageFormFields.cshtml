@model PageViewModel

<div class="row g-3">
    <div class="col-md-8">
        <div class="card mb-3">
            <div class="card-header">
                <h3 class="card-title">Thông tin cơ bản</h3>
            </div>
            <div class="card-body">
                <div class="form-group mb-3">
                    <label asp-for="Title" class="form-label required"></label>
                    <div class="input-group">
                        <span class="input-group-text">
                            <i class="ti ti-file-text"></i>
                        </span>
                        <input asp-for="Title" class="form-control" placeholder="Tiêu đề trang" />
                    </div>
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="Slug" class="form-label required"></label>
                    <div class="input-group">
                        <span class="input-group-text">
                            <i class="ti ti-link"></i>
                        </span>
                        <input asp-for="Slug" class="form-control" placeholder="ten-trang-than-thien" />
                        <button class="btn btn-outline-secondary" type="button" id="generateSlugButton"
                                title="Tạo slug tự động">
                            <i class="ti ti-refresh"></i>
                        </button>
                    </div>
                    <span asp-validation-for="Slug" class="text-danger"></span>
                    <small class="form-hint">
                        <i class="ti ti-info-circle me-1"></i>Dùng cho URL (chỉ chứa chữ cái thường, số và dấu gạch ngang)
                    </small>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="Content" class="form-label required"></label>
                    <textarea asp-for="Content" class="form-control wysiwyg" rows="15"
                              placeholder="Nội dung chi tiết trang..."></textarea>
                    <span asp-validation-for="Content" class="text-danger"></span>
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-4">
         <div class="card mb-3">
             <div class="card-header">
                 <h3 class="card-title">Thiết lập</h3>
             </div>
             <div class="card-body">
                 <div class="form-group mb-3">
                     <label asp-for="Status" class="form-label required"></label>
                     <select asp-for="Status" asp-items="@Html.GetEnumSelectList<PublishStatus>()" class="form-select"></select>
                     <span asp-validation-for="Status" class="text-danger"></span>
                 </div>

                 <div class="form-group mb-3">
                     <label asp-for="PublishedAt" class="form-label"></label>
                     <input asp-for="PublishedAt" type="text" class="form-control datepicker" />
                     <span asp-validation-for="PublishedAt" class="text-danger"></span>
                      <small class="form-hint">
                         Ngày và giờ trang được xuất bản. Nếu để trống, sẽ lấy thời gian hiện tại khi chuyển trạng thái sang "Đã xuất bản".
                     </small>
                 </div>
             </div>
         </div>
    </div>

    <div class="col-md-12">
        <partial name="Components/Crud/_SeoFormFields" model="Model" />
    </div>
</div>
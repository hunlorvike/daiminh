<div class="modal modal-blur fade" id="selectMediaModal" tabindex="-1" role="dialog" aria-labelledby="selectMediaModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="selectMediaModalLabel">
                    <i class="ti ti-photo-circle me-2"></i>Chọn Media
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body p-3"> 
                <div id="select-media-modal-body-content">
                    <div class="text-center p-5">
                        <div class="spinner-border text-primary" role="status"></div>
                        <p class="mt-2 text-muted">Đang tải thư viện...</p>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <div class="w-100 d-flex justify-content-between align-items-center">
                    <div id="modal-selected-file-info" class="text-muted small d-flex align-items-center gap-1" style="display: none;">
                       <i class="ti ti-file-check text-primary"></i> 
                       Đã chọn: <strong id="selected-file-name" class="text-truncate" style="max-width: 200px; display: inline-block;"></strong>
                    </div>
                    <div>
                        <button type="button" class="btn" data-bs-dismiss="modal">Đóng</button>
                        <button type="button" class="btn btn-primary ms-2" id="modal-select-file-btn" disabled>
                            <i class="ti ti-check me-1"></i>Chọn
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
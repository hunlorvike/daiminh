@model web.Areas.Admin.ViewModels.RoleViewModel

<div class="row g-3">
	<div class="col-md-8">
		<div class="form-group mb-3">
			<label asp-for="Name" class="form-label required"></label>
			<div class="input-group">
				<span class="input-group-text">
					<i class="ti ti-user-shield"></i>
				</span>
				<input asp-for="Name" class="form-control" placeholder="Ví dụ: Administrator, Editor, Member..." />
			</div>
			<span asp-validation-for="Name" class="text-danger"></span>
		</div>

		<div class="form-group mb-3">
			<label asp-for="SelectedClaimDefinitionIds" class="form-label"></label>
			<select asp-for="SelectedClaimDefinitionIds" asp-items="@Model.AvailableClaimDefinitions"
					class="form-select tom-selected" autocomplete="off"  multiple data-placeholder="Chọn các quyền hạn cho vai trò này">
			</select>
			<span asp-validation-for="SelectedClaimDefinitionIds" class="text-danger"></span>
			<small class="form-hint">
				<i class="ti ti-info-circle me-1"></i>Chọn các quyền hạn mà vai trò này sẽ có.
			</small>
		</div>
	</div>
	<div class="col-md-4">
		<div class="card">
			<div class="card-header">
				<h3 class="card-title">
					<i class="ti ti-info-circle me-2"></i>Thông tin về Vai trò và Quyền hạn
				</h3>
			</div>
			<div class="card-body">
				<p>
					**Vai trò (Role):** Là một nhóm các quyền hạn. Thay vì gán từng quyền hạn cho từng người dùng, bạn gán người dùng vào các vai trò.
				</p>
				<p>
					**Quyền hạn (Claim Definition):** Là một quyền cụ thể (ví dụ: `Product.View`, `User.Create`). Vai trò được cấu thành từ một tập hợp các quyền hạn này.
				</p>
				<ul class="list-unstyled mb-0">
					<li class="d-flex align-items-center mb-2">
						<i class="ti ti-check text-success me-2"></i>
						<span>Đặt tên vai trò rõ ràng, dễ hiểu.</span>
					</li>
					<li class="d-flex align-items-center mb-2">
						<i class="ti ti-check text-success me-2"></i>
						<span>Gán các quyền hạn phù hợp với mục đích của vai trò.</span>
					</li>
				</ul>
			</div>
		</div>
	</div>
</div>
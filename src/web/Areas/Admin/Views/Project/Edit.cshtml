@using web.Areas.Admin.ViewModels.Project
@model ProjectViewModel
@{
    ViewData["Title"] = "Chỉnh sửa Dự án - Hệ thống quản trị";
}

@section Styles {
 <style>/* Add specific styles from Product Edit */</style>
 }

<form asp-action="Edit" asp-route-id="@Model.Id" method="post" id="project-form">
    <div class="card">
        <div class="card-header"><h3 class="card-title">Thông tin Dự án</h3></div>
        <div class="card-body">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <div asp-validation-summary="ModelOnly" class="text-danger alert alert-danger mb-3"></div>
            <partial name="_ProjectFormFields" model="Model" />
        </div>
        <div class="card-footer text-end bg-transparent border-top pt-3">
            <a asp-action="Index" class="btn btn-link">Hủy</a>
            <button type="submit" class="btn btn-primary ms-2"><i class="ti ti-device-floppy me-2"></i> Cập nhật Dự án</button>
        </div>
    </div>
</form>

@section Scripts {
    <script src="~/js/slug.js" asp-append-version="true"></script>
    @* Include TomSelect & Summernote JS if not global *@
    <script> $(document).ready(function() { /* --- PASTE FULL JS from Article/Product Edit --- */ /* Adapt IDs like #projectName, #projectSlug, #selectProjectImagesBtn, #project-image-list, #project-image-template */ }); </script>
}
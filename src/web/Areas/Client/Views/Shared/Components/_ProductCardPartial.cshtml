@model web.Areas.Client.ViewModels.ProductCardViewModel

<div class="card card-sm h-100">
	<a href="@Url.Action("Detail", "Product", new { slug = Model.Slug })" class="d-block">
		<img src="@Model.ThumbnailUrl" alt="@Model.Name" class="card-img-top" style="height: 180px; object-fit: cover;" />
	</a>
	<div class="card-body d-flex flex-column">
		<h3 class="h5 mb-1 flex-grow-1">
			<a href="@Url.Action("Detail", "Product", new { slug = Model.Slug })" class="text-reset">@Model.Name</a>
		</h3>
		<div class="mt-auto">
			<a href="@Url.Action("Detail", "Product", new { slug = Model.Slug })" class="btn btn-primary w-100">
				<i class="ti ti-eye me-1"></i>Xem chi tiết
			</a>
		</div>
	</div>
</div>